{
  "name": "image-recognition-app",
  "version": "1.0.0",
  "description": "Image recognition app using Clarifai",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install && echo 'Build complete'"
  },
  "dependencies": {
    "axios": "*",
    "dotenv": "*",
    "express": "*",
    "multer": "*"
  }
}
